# ITU-Calculator

Small calculator for ITU-R P.838-3 rain-attenuation coefficients.

The calculator is useful for checking the frequency- and polarization-dependent coefficients used by the attenuation forward model in `Compute-Link-Attenuations/`.

The maintained app code is in `Python-React/`.
