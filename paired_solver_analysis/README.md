# Paired Solver Analysis

Auxiliary scripts and outputs for paired solver comparisons.

These analyses are downstream of the maintained `HundredPatches/pipeline/` outputs and are used to compare solvers patch-by-patch rather than only through aggregate descriptive statistics.
