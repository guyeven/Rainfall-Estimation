[project]
name = "rainfall-estimation-research"
version = "0.1.0"
description = "Rainfall reconstruction experiments using commercial microwave links"
requires-python = ">=3.11,<3.15"
license = "CC-BY-4.0"

[tool.pytest.ini_options]
testpaths = ["Compute-Link-Attenuations/tests"]
