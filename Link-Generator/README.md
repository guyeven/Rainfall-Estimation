# Link-Generator

Utilities for generating and inspecting microwave-link geometries.

- `backend/` contains the Python backend code.
- `frontend/` contains a Vite/React interface for visual inspection.

This tool is upstream of the rainfall-reconstruction pipeline: generated or inspected link geometries can be used to build estimator inputs consumed later by `Compute-Link-Attenuations/`.
