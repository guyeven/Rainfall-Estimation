# Link-Generator Frontend

React/Vite frontend for inspecting link geometries.

```bash
npm ci
npm run dev
```

Run it together with the backend in `../backend/` when interactive data loading is needed.
